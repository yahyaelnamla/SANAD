"use client";

import { ThemeSelector } from "@/components/ThemeSelector";

/** Compact theme picker for header and toolbars. */
export function ThemeToggle() {
  return <ThemeSelector variant="icon" />;
}
