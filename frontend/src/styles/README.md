# Styles

Global styles live in **`src/app/globals.css`** (Tailwind directives, CSS variables, theme tokens).

Tailwind configuration: **`frontend/tailwind.config.ts`** at the frontend project root.

This directory is reserved for future shared style modules. Do not duplicate `globals.css` here.
